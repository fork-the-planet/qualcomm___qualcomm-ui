---
title: Table Sizes
component: Table
---

import {Demo} from "~components/demo"
import * as demos from "./demos"

# {frontmatter.title}

::: meta
keywords: [table density options, compact table rows, table row spacing, small medium large table]
:::

## Sizes

There are three sizes available for the table component: `sm`, `md`, and `lg`. The default size is `md`.

<Demo name="SizesDemo" component={demos.SizesDemo} />
