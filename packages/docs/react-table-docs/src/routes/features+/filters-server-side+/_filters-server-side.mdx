---
title: Filters (Server-side)
component: Table
---

import {TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"
import {Demo} from "~components/demo"
import * as demos from "./demos"

# {frontmatter.title}

::: meta
keywords: [remote data filtering, API filter example, manual table filtering, ColumnFiltersState, onColumnFiltersChange, pageCount, TanStack Query integration]
:::

This page features a server-side filtering, pagination, and sorting demo with a simulated backend API. State is managed in the component and sent to the server on each change. This is the standard approach for backend integrations where the server handles filtering, pagination, and sorting logic. See the [Filters Guide](/feature-guides/column-filtering) for implementation details.

<Demo
  expanded
  name="FiltersServerSideDemo"
  component={demos.FiltersServerSideDemo}
/>
