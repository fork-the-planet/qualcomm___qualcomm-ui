---
title: Filters (Client-side)
component: Table
---

import {TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"
import {Demo} from "~components/demo"
import * as demos from "./demos"

# {frontmatter.title}

::: meta
keywords: [table search example, column filter, global search filter, ColumnFilterAction, setFilterValue, getFilterValue, getPreFilteredRowModel, debounced filter]
:::

The following example demonstrates clientside filters for a table with 100,000 rows of data.

<Demo
  expanded
  name="FiltersClientSideDemo"
  component={demos.FiltersClientSideDemo}
/>
