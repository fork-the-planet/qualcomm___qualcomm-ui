import type {ReactElement} from "react"

import {Switch} from "@qualcomm-ui/react/switch"

export function SwitchHintDemo(): ReactElement {
  return (
    // preview
    <Switch hint="You can change this later" label="Enable notifications" />
    // preview
  )
}
